<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar producto</title>
</head>
<body>
    <h1>Elige un producto</h1>
    <form action="/products/<%= product._id %>?_method=PUT" method="POST">
        <label for="name">Nombre de producto</label>
        <input type="text" name="name" placeholder="nombre del producto" id="name" value="<%= product.name %>">
        <label for="price">Precio del producto</label>
        <input type="number" name="price" id="price" placeholder="precio del producto" value="<%= product.price %>">
        <label for="category">Selecciona Categoria</label>
        <select name="category" id="category">
            <option value="fruit" <%= product.category === 'fruit' ? 'selected': '' %> >Fruta</option>
            <option value="vegetable" <%= product.category === 'vegetable' ? 'selected': '' %>>Verdura</option>
            <option value="dairy" <%= product.category === 'dairy' ? 'selected': '' %>>Lacteos</option> -->
            
        </select>
        <button>Agregar producto</button>
        
    </form>
    <a href="/products/<%= product._id %>">Cancelar</a>
</body>
</html>